{
  "kind": "collectionType",
  "collectionName": "organizations",
  "info": {
    "name": "organization",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "name": {
      "type": "string"
    },
    "events": {
      "via": "organization",
      "collection": "event"
    },
    "tasks": {
      "collection": "task",
      "via": "organization"
    },
    "customers": {
      "via": "organizations",
      "collection": "customer"
    },
    "locations": {
      "collection": "location",
      "via": "organization"
    }
  }
}
